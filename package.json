{
  "name": "spotify-to-rekordbox",
  "version": "0.1.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "start": "ts-node src/runLuckyForTracklist.ts",
    "test": "jest --runInBand --reporters=default",
    "lint": "git ls-files '*.js' '*.cjs' | xargs -n1 node --check"
  },
  "bin": {
    "run-lucky": "script/run-lucky"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "18",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}

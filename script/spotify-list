#!/usr/bin/env bash
# Exec the TypeScript entrypoint using the ts-node CLI
SCRIPT_DIR="$(cd -- "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
TS_NODE="${TS_NODE:-ts-node}"
exec env TS_NODE_TRANSPILE_ONLY=1 "$TS_NODE" "$SCRIPT_DIR/../src/spotify_list.ts" "$@"

#!/usr/bin/env bash
# Exec the TypeScript entrypoint using ts-node ESM loader
SCRIPT_DIR="$(cd -- "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
NODE="${NODE:-node}"
exec "$NODE" --loader ts-node/esm "$SCRIPT_DIR/../src/spotify_list.ts" "$@"

#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running pre-push checks..."

# Lint (no autofix on push), formatting check, typecheck, and tests
yarn -s lint || exit 1
yarn -s format:check || exit 1
yarn -s typecheck || exit 1
yarn -s test --runInBand || exit 1

